/// Run the enemy's turn.

draw_set_color(c_white);
draw_text(250, 450, "NOW WOULD BE THE SYNC TURN!");

/* The enemy object, when it is done, will handle this. */
//obj_battle.currentBattleState = 0;
//turnIsRunning = 0;
